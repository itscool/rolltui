│ Wrap engine                   │ build       │ single header over the Unicod
│                               │             │ header
│ Windows, layers, placement    │ build       │ single header;
│ (Dim abs/rel per axis), focus │             │ resolve(Placement, parent) →
│                               │             │ Rect is a pure function
│ Menu widget (hierarchical,    │ build       │ single header each; the widge
│ filterable) + list/text       │             │ set is deliberately this smal
│ widgets                       │             │
│ Layout files (JSON: windows + │ build       │ reuses the theme loader's han
│ placements + content slots)   │             │ parser; hot-reloaded
│ Markdown block renderer       │ build       │ single header over md4c + wra
│ (blocks → styled wrapped     │             │
│ lines)                        │             │
│ Theme (roles → styles, JSON  │ build       │ single header
│ loader, built-ins)            │             │
└───────────────────────────────┴───────

Here is the function you asked for:

┌ cpp ────────────────────────────────────
│ int add(int a, int b) {
│   return a + b;
└───────────────────────────────────────
 mono  80x24  line 91/113  follow  truecolor
